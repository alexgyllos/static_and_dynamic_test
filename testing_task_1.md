### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby

class CardGame


  def checkforAce(card)                     #def check_for_ace(card)
    if card.value = 1                       # if card.value == 1
      return true                           #  return true
    else                                    # else
      return false                          #  return false
    end                                     # end
  end                                       #end

  dif highest_card(card1 card2)             #def highest_card(card1, card2)
  if card1.value > card2.value              # if card1.value > card2.value
    return card                             #  return card1
  else                                      # else
    return card2                            #  return card2
  end                                       # end
end                                         #end
end                                         #unnecessary end

def self.cards_total(cards)                 #def self.cards_total(cards)
  total                                     # total = 0
  for card in cards                         #  for card in cards
    total += card.value                     #   total +=
    return "You have a total of" + total    #  end
  end                                       #  return "You have a total of " + "#{total}"
end                                         #end
                                            #end(for class)
```
